import 'package:flutter/material.dart';

class OtherExamplePage extends StatelessWidget {
  const OtherExamplePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Other"),
      ),
    );
  }
}
